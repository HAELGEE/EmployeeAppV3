@{
    ViewBag.Title = "Lönespec";    
}

<h3>Här visas din lönespec</h3>

<ul>
    <li>
        <h4>Anställnings nummer: @Model.EmployeeService.GetEmployeeById(ViewBag.Id).Id</h4>
    </li>
    <li>
        <h4>Namn: @Model.EmployeeService.GetEmployeeById(ViewBag.Id).Name</h4>
    </li>

    <li>
        <h4>Du har @Model.EmployeeService.GetEmployeeById(ViewBag.Id).Salary kr/tim</h4>
    </li>
    <li>
        <h4> Arbetad tid: @Model.EmployeeService.GetEmployeeById(ViewBag.Id).WorkedTime timmar</h4>
    </li>

</ul>
<br />

<hr />
<h4>Lön: @Model.EmployeeService.GetEmployeeById(ViewBag.Id).MonthSalary kr</h4>